Regex patterns can be used to ensure emails, names have only the characters that the field's business logic requires.

Regex is good to augment other forms of validation - like min/ max character levels etc.