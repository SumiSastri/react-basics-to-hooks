{
  "name": "react-basics-to-hooks",
  "version": "1.0.0",
  "npmClient": "npm",
  "devDependencies": {
    "lerna": "^5.0.0"
  },
  "scripts": {
    "new-version": "lerna version --conventional-commits --yes",
    "diff": "lerna diff", 
    "test": "lerna run test --scope={breaking-bad,videorama}"
  }
}
