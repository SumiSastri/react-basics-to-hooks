import React from "react";
import SetAndClearIntervals from "../classComponents/lifecycleMethods/SetAndClearIntervals";

const Home = () => (
  <div>
    <h1>
      This is a React tutorial which refactors all class components to Hooks
    </h1>
    <SetAndClearIntervals />
  </div>
);
export default Home;
